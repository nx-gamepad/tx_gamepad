name: tx_gamepad
description: Client example new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  n_gamepad: ^0.0.4
  t_gamepad:
    git:
      url: https://github.com/devEnju/t_gamepad.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
